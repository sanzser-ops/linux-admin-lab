---
- name: Install common administration tools
  hosts: linux_servers
  become: true

  tasks:
    - name: Ensure curl is installed
      ansible.builtin.package:
        name: curl
        state: present
